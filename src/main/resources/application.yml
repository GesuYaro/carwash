spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/test
    username: user
    password: user
    driverClassName: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    user: user
    password: user
    url: jdbc:postgresql://localhost:5434/test


